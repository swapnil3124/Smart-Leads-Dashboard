version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGO_URI=mongodb+srv://swapnilpasalkar1081_db_user:vohKowWbuThSxwRh@smartleaddashboard.12kmfig.mongodb.net/?appName=SmartLeadDashboard
      - JWT_SECRET=supersecretkey

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
